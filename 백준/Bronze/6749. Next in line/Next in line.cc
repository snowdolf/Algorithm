#include <iostream>
#include <vector>

using namespace std;

int a, b;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    cin >> a >> b;

    cout << 2 * b - a;

    return 0;
}
